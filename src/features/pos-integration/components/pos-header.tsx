import Link from "next/link";
import { ArrowLeft, PlugZap } from "lucide-react";
export function PosHeader({ title, description, back = "/pos-integration" }: { title: string; description: string; back?: string }) { return <header className="mb-8 flex items-start gap-4"><Link href={back} className="flex size-10 shrink-0 items-center justify-center rounded-xl border border-border bg-white text-muted"><ArrowLeft className="size-4" /></Link><span className="hidden size-10 items-center justify-center rounded-xl bg-cyan-50 text-cyan-700 sm:flex"><PlugZap className="size-4" /></span><div><p className="text-xs font-bold uppercase tracking-[0.14em] text-primary">POS integration</p><h1 className="mt-2 text-2xl font-bold sm:text-3xl">{title}</h1><p className="mt-2 text-sm text-muted">{description}</p></div></header>; }
